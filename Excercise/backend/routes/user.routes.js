// imports
const express = require("express");

// Create a router
const router = express.Router();

// Login Route
router.post("/login", () => {});

// Sign-Up Route
router.post("/signup", () => {});

// Exports
module.exports = router;
